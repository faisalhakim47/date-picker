{
  "name": "range-datetime-picker",
  "version": "1.0.0",
  "description": "Range Date time picker",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json && esbuild --bundle --minify --sourcemap --format=esm --outfile=dist/index.bundle.min.js dist/index.js",
    "dev": "web-dev-server",
    "test": "test",
    "format": "eslint --fix src"
  },
  "author": "",
  "license": "ISC",
  "types": "src/elements.d.ts",
  "devDependencies": {
    "@web/dev-server": "^0.4.1",
    "esbuild": "^0.20.0",
    "eslint": "^8.56.0",
    "eslint-plugin-import": "^2.29.1",
    "typescript": "^5.3.3"
  }
}
